void main() {
int n = readInteger("Entrez les dimensions de la croix");
    String[][] croix = new String [n][n];

for (int a = 0 ; a < croix.length; a++){
for(int b = 0 ; b <croix[a].length ; b++){
        croix[a][b] = "X";}
        }   

for(int i = 0 ; i <= (n+1)/2 ; i++ ) {
    
}

        
}